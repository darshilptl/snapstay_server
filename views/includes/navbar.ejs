<nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm border-bottom sticky-top">
  <div class="container-fluid">
    
    <!-- Brand/Logo -->
    <a class="navbar-brand d-flex align-items-center" href="/listings">
      <img src="/icons/snapstay.png" alt="SnapStay Logo" width="30" height="30" class="d-inline-block align-text-top me-2">
      <span class="fw-bold">SnapStay</span>
    </a>

    <!-- Toggler for mobile view -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar Links -->
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <!-- Explore Link with Icon -->
        <a class="nav-link d-flex align-items-center" href="/listings">
          <i class="fa-solid fa-map-marker-alt me-2"></i> Explore
        </a>
      </div>
      <div class="navbar-nav ms-auto">
        <!-- Add Listing with Icon -->
        <a class="nav-link d-flex align-items-center" href="/listings/new">
          <i class="fa-solid fa-plus-circle me-2"></i> Add your Listing
        </a>
         <!-- Contact Us Button -->
         <a class="btn btn-outline-secondary btn-sm mx-2 d-flex align-items-center animate-btn" href="/contact">
          <i class="fa-solid fa-envelope me-2"></i> Contact Us
        </a>
        <!-- Conditionally rendered links -->
        <% if (!currUser) { %>
        <!-- Sign Up Button -->
        <a class="btn btn-outline-secondary btn-sm mx-2 d-flex align-items-center animate-btn" href="/signup">
          <i class="fa-solid fa-user-plus me-2"></i> Sign up
        </a>
        <!-- Log In Button -->
        <a class="btn btn-outline-secondary btn-sm mx-2 d-flex align-items-center animate-btn" href="/login">
          <i class="fa-solid fa-sign-in-alt me-2"></i> Log in
        </a>
        <% } %>
        <% if (currUser) { %>
        <!-- Log Out Button -->
        <a class="btn btn-danger btn-sm mx-2 d-flex align-items-center animate-btn" href="/logout">
          <i class="fa-solid fa-sign-out-alt me-2"></i> Log out
        </a>
        <% } %>
      </div>
    </div>
  </div>
</nav>

<style>
  .navbar {
    transition: background-color 0.3s ease-in-out;
  }

  /* Hover effects for links */
  .nav-link {
    color: #555;
    transition: color 0.2s ease;
  }

  .nav-link:hover {
    color: #007bff;
  }

  /* Buttons with animations */
  .btn {
    padding: 0.5rem 1rem;
    border-radius: 30px;
    font-weight: bold;
    display: inline-flex;
    align-items: center;
    transition: all 0.3s ease;
  }

  .btn i {
    font-size: 1.2em;
  }

  /* Button hover animations */
  .btn-primary:hover {
    background-color: #0056b3;
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(0, 123, 255, 0.4);
  }

  .btn-outline-primary:hover {
    /* color: #fff; */
    /* background: rgba(255, 255, 255, 0.2); */
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(108, 117, 125, 0.4);
  }

  .btn-danger:hover {
    background-color: #c82333;
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(220, 53, 69, 0.4);
  }

  .btn-outline-secondary:hover {
    color: #fff;
    background-color: #6c757d;
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(108, 117, 125, 0.4);
  }

  /* Button animation */
  .animate-btn {
    position: relative;
    overflow: hidden;
    z-index: 1;
  }

  .animate-btn::after {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: rgba(255, 255, 255, 0.2);
    transition: all 0.4s ease;
    z-index: -1;
  }

  .animate-btn:hover::after {
    left: 100%;
  }

  /* Navbar shadow on scroll */
  .navbar.sticky-top {
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  }

  .navbar-toggler {
    border-color: rgba(0, 0, 0, 0.1);
  }

  .navbar-toggler-icon {
    filter: invert(50%);
  }

  /* Icon adjustments */
  .nav-link i, .btn i {
    font-size: 1.2em;
    color: #007bff;
  }

  .navbar-brand i {
    font-size: 1.5em;
  }
</style>
